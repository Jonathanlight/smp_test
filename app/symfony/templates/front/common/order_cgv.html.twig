<!-- Modal -->
{% set page = constant('App\\Entity\\Page::CODE_CGV')|getPage %}
<div class="modal fade" id="orderCgv" tabindex="-1" role="dialog" aria-labelledby="orderCgvLabel" aria-hidden="true">
  <div class="modal-dialog" role="document">
    <div class="modal-content">
      <div class="modal-header">
        <h5 class="modal-title" id="orderCgvLabel">{{ page.title }}</h5>
        <button type="button" class="close" data-dismiss="modal" aria-label="Close">
          <span aria-hidden="true">&times;</span>
        </button>
      </div>
      <div class="modal-body">
          {% for block in page.blocks %}
              {{ block.content|raw }}
          {% endfor %}
      </div>
    </div>
  </div>
</div>
